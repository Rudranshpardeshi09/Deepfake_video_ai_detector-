/**
 * Custom React hooks for the AI video detection application.
 *
 * These hooks provide reusable logic for:
 * - Video file upload and validation
 * - Analysis API communication
 * - Theme management
 */

export { useVideoUpload } from './useVideoUpload';
export { useAnalysis } from './useAnalysis';

export type { };
